// Test stub for `src/lib/trpData.ts`
// This file is a starting point. Run with Vitest or Jest after installing the test runner.

import assert from 'assert'
// import { addEntity, findEntityByName, linkEntities, mergeEntities, createClientFromEntity, getState } from '../lib/trpData'

// Example (pseudocode):
// describe('trpData entity helpers', () => {
//   it('adds and finds entity by name', () => {
//     const e = addEntity({ name: 'Alice Example', type: 'person' })
//     const found = findEntityByName('Alice Example')
//     assert.ok(found && found.id === e.id)
//   })
//
//   it('links two entities', () => {
//     const a = addEntity({ name: 'A' })
//     const b = addEntity({ name: 'B' })
//     linkEntities(a.id, b.id, 'mentioned_with')
//     const s = getState()
//     assert.ok(s.entities[a.id].links.some(l => l.targetId === b.id))
//   })
//
//   // add more tests per plan in docs/STRATEGY_TESTS.md
// })

console.log('trpData.test.ts is a stub. Follow docs/STRATEGY_TESTS.md to wire up tests.')
